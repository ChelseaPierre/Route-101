//image speed assignment, includes how fast frames are
//being played
image_speed = 3;
//The speed assignment
gSpeed = 3;
//Collision 
cSpd = gSpeed + 2;

//Optional alternative keyboard movements variables
//left = keyboard_check(vk_left);
//right = keyboard_check(vk_right);
//up = keyboard_check(vk_up);
//down = keyboard_check(vk_down);